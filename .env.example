# PostgreSQL (локальный запуск; в Docker адрес другой)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/postgres

# JWT
JWT_SECRET_KEY=change-me-to-a-long-random-string
JWT_ALGORITHM=HS256
JWT_EXPIRES_MINUTES=60
